import React from 'react';

const Input = () => {
  // We will add state and handlers here on Day 6.
  // For now, it's just the visual structure.
  return (
    <div>
      <span style={{ color: '#4AF626' }}>&gt; </span>
      {/* The blinking cursor will be added later with CSS */}
    </div>
  );
};

export default Input;